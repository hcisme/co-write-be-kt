spring:
  config:
    activate:
      on-profile: prod

  datasource:
    username: cowrite
    password: NznxFrrbepLaW5fC
    url: jdbc:mysql://127.0.0.1:3306/cowrite?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      pool-name: HikariCPDatasource
      minimum-idle: 5
      idle-timeout: 180000
      maximum-pool-size: 10
      auto-commit: true
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1

  data:
    redis:
      database: 1
      host: 127.0.0.1
      port: 6379
      password: emmmm000
      jedis:
        pool:
          max-active: 20
          max-wait: -1
          max-idle: 10
          min-idle: 0
      connect-timeout: 2000

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

knife4j:
  enable: false

log:
  path: /www/chihaicheng/CoWriteBeSpringboot
  root:
    level: info

node:
  server:
    url: http://127.0.0.1:8087
    internal-secret: 807aa60b-a3e9-4130-a63a-f1623e652a83
